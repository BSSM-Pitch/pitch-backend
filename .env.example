# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pitch
DB_USERNAME=pitch
DB_PASSWORD=changeme

# JWT (HS256 서명용 비밀키, 충분히 길고 랜덤한 값으로 교체할 것)
JWT_SECRET=change-this-to-a-long-random-secret-key-at-least-32-bytes

# 파일 스토리지 (로컬 디스크 경로, 추후 S3/Supabase 교체 시 이 값만 변경)
STORAGE_PATH=./uploads
